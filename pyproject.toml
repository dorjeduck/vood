[project]
name = "vood"
version = "0.1.0"
description = "Vood library"
requires-python = ">=3.9"
license = {text = "MIT"}

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["vood"]